<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Class="ChatApp.WPF.MainWindow"
    mc:Ignorable="d"
    Title="MainWindow" Height="360" Width="640" MinWidth="640" MinHeight="120" Loaded="Window_Loaded" Topmost="True">

    <Grid>
        <DockPanel Width="180" HorizontalAlignment="Left" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
            <DockPanel Margin="10,0,10,0" Height="30">
                <Label>팟플레이어</Label>
                <Button Content="선택" Click="SelectPlayerButton_OnClick">
                    <Button.ContextMenu>
                        <ContextMenu Name="MyContextMenu">
                        </ContextMenu>
                    </Button.ContextMenu>
                 </Button>
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>